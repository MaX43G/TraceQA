# ============================================================
# 溯知 / TraceQA 网关配置（本地最小化）
# 完整配置已迁移至 Nacos 配置中心
# ============================================================
server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: traceqa-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
