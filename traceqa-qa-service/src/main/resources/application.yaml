# ============================================================
# 溯知 / TraceQA 问答服务配置（本地最小化）
# 完整配置已迁移至 Nacos 配置中心
# ============================================================
server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: traceqa-qa-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
