# ============================================================
# 溯知 / TraceQA 管理服务配置（本地最小化）
# 完整配置已迁移至 Nacos 配置中心
# ============================================================
server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: traceqa-admin-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
